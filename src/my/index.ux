<template>
  <div class="my-wrapper">
    <div class="header">
      <div class="user-info">
        <div class="avatar-wrapper">
          <image
            class="avatar"
            src="/assets/images/logo.png"
            mode="aspectFill"
          />
        </div>
        <div class="user-text">
          <text class="username">美食达人</text>
          <text class="user-desc">发现美味，轻松下厨</text>
        </div>
      </div>
    </div>

    <div class="agreement-section">
      <div class="agreement-item" @click="openPrivacy">
        <text class="agreement-text">隐私协议</text>
        <text class="agreement-arrow">›</text>
      </div>
      <div class="agreement-item" @click="openUserAgreement">
        <text class="agreement-text">用户协议</text>
        <text class="agreement-arrow">›</text>
      </div>
      <div class="agreement-item" @click="openContact">
        <text class="agreement-text">联系我们</text>
        <text class="agreement-arrow">›</text>
      </div>
    </div>
  </div>
</template>

<script>
import router from '@system.router'

export default {
  data: {
    favorites: [],
    downloads: [],
    history: []
  },

  onInit() {
   
  },

  onShow() {
   
  },

  openPrivacy() {
    router.push({
      uri:'my/primary'
    })
  },

  openUserAgreement() {
     router.push({
      uri:'my/userAgreement'
    })
  },

  openContact() {
    router.push({
      uri:'my/contact'
    })
  }
}
</script>

<style lang="less">
@import '../assets/styles/style.less';

.my-wrapper {
  width: 100%;
  flex: 1;
  flex-direction: column;
  background-color: #f5f5f5;
}

.header {
  background: linear-gradient(135deg, #4ecdc4 0%, #44a08d 100%);
  padding: 60px 30px 40px;
  flex-direction: column;

  .user-info {
    display: flex;
    align-items: center;
    margin-bottom: 30px;

    .avatar-wrapper {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      overflow: hidden;
      border: 4px solid rgba(255, 255, 255, 0.3);

      .avatar {
        width: 100%;
        height: 100%;
      }
    }

    .user-text {
      flex: 1;
      margin-left: 25px;
      flex-direction: column;

      .username {
        font-size: 36px;
        font-weight: bold;
        color: #fff;
      }

      .user-desc {
        font-size: 26px;
        color: rgba(255, 255, 255, 0.8);
        margin-top: 8px;
      }
    }
  }

  .stats-row {
    display: flex;
    justify-content: space-around;
    background-color: rgba(255, 255, 255, 0.15);
    border-radius: 16px;
    padding: 25px 0;

    .stat-item {
      flex-direction: column;
      align-items: center;

      .stat-num {
        font-size: 40px;
        font-weight: bold;
        color: #fff;
      }

      .stat-label {
        font-size: 24px;
        color: rgba(255, 255, 255, 0.8);
        margin-top: 8px;
      }
    }
  }
}

.menu-section {
  margin: 20px 15px;
  background-color: #fff;
  border-radius: 16px;
  flex-direction: column;

  .menu-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 30px;
    border-bottom: 1px solid #f0f0f0;

    &:last-child {
      border-bottom: none;
    }

    .menu-icon {
      font-size: 36px;
      margin-right: 20px;
    }

    .menu-text {
      flex: 1;
      font-size: 28px;
      color: #333;
    }

    .menu-arrow {
      font-size: 36px;
      color: #ccc;
    }
  }
}

.agreement-section {
  margin: 20px 15px;
  background-color: #fff;
  border-radius: 16px;
  flex-direction: column;

  .agreement-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 30px;
    border-bottom: 1px solid #f0f0f0;

    &:last-child {
      border-bottom: none;
    }

    .agreement-text {
      font-size: 28px;
      color: #666;
    }

    .agreement-arrow {
      font-size: 36px;
      color: #ccc;
    }
  }
}
</style>